
/*package movie.db.client;

import static org.junit.Assert.*;

import java.util.ArrayList;
import java.util.List;

import org.junit.Test;

import com.google.gwt.junit.client.GWTTestCase;*/
/**
 * @author Christoph Weber
 * Tests the arrayLitToStringConverter of the ResultTable class,
 * if it works properly. Checks it with entries and without.
 */
/*public class ArrayListToStringConverterTest extends GWTTestCase {
	
	@Test
	public void test() {
		ArrayList<String> testList = new ArrayList<String>();
		testList.add("English");testList.add("Japanese");
		testList.add("German");testList.add("French");
		
		assertEquals("English",testList.get(0).toString());
		assertEquals("Japanese",testList.get(1).toString());
		assertEquals("German",testList.get(2).toString());
		assertEquals("French",testList.get(3).toString());
		
		assertEquals(ResultTable.arrayListToStringConverter(testList), " English, Japanese, German, French");
		
		System.out.println(ResultTable.arrayListToStringConverter(testList));
		
		testList = null;
		assertNull(null,testList);
		
	}

	@Override
	public String getModuleName() {
		return "movie.db.Error404";
	}

}*/
